# ~/etc/bash/early/misc
# -*- Mode: Bash; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 2 -*-
# 
# miscellaneous settings

export     VISUAL='vi'
[ -x ${HOME}/bin/mate ]              && VISUAL="mate -w"

export     EDITOR='vi'
[ -x ${HOME}/bin/mate ]              && EDITOR="mate -w"
