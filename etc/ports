# ~/etc/ports

 STORE=PowerMac
   DEV=rockboard-lm


# essentials
*               bash-completion

# source control
*               perl5.8             # no-variants (pre-req of git-core)
*               curl                # no-variants (pre-req of git-core)
*               libiconv            # no-variants (pre-req of git-core)
*               git-core            +bash_completion
*               db46                # no-variants (pre-req of subversion)
*               apr-util            # no-variants (pre-req of subversion)
*               cyrus-sasl2         # no-variants (pre-req of subversion)
*               subversion          +bash_completion

# ~/bin prereqs
*               p5-readonly         # (pre-req of `pcp` and `pmv`, etc.)
*               p5-io-interactive   # (pre-req of `pcp` and `pmv`, etc.)