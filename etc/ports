# ~/etc/ports

STORE=PowerMac
  DEV=rockboard-lm
MEDIA=PowerMac


# essentials
*               bash-completion

# source control
*               perl5.8             # no-variants (pre-req of git-core)
*               curl                # no-variants (pre-req of git-core)
*               libiconv            # no-variants (pre-req of git-core)
*               git-core            +bash_completion
DEV             db46                # no-variants (pre-req of subversion)
DEV             apr-util            # no-variants (pre-req of subversion)
DEV             cyrus-sasl2         # no-variants (pre-req of subversion)
DEV             subversion          +bash_completion

# ~/bin prereqs
*               p5-readonly         # (pre-req of `pcp` and `pmv`, etc.)
*               p5-io-interactive   # (pre-req of `pcp` and `pmv`, etc.)
*               p5-version          # (pre-req of `pcp` and `pmv`, etc.)

